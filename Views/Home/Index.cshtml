@{
    ViewData["Title"] = "Ana Sayfa";
}

<style>
    :root {
        --primary-color: #2c5aa0; /* Bingöl Üniversitesi mavisi */
        --secondary-color: #e9f2f9;
        --accent-color: #d4af37; /* Altın rengi aksan */
        --text-dark: #333;
        --text-light: #555;
        --white: #ffffff;
        --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        --transition: all 0.3s ease;
    }

    .hero-section {
        background: linear-gradient(135deg, var(--primary-color) 0%, #1a3a6d 100%);
        color: var(--white);
        padding: 80px 0;
        border-radius: 0 0 30px 30px;
        margin-bottom: 60px;
        position: relative;
        overflow: hidden;
    }

    .hero-section::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" preserveAspectRatio="none"><path d="M0,0 Q500,100 1000,0 L1000,100 L0,100 Z" fill="rgba(255,255,255,0.05)"/></svg>');
        background-size: 100% 100px;
        background-position: bottom;
    }

    .hero-title {
        font-size: 3.2rem;
        font-weight: 800;
        margin-bottom: 20px;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
    }

    .hero-subtitle {
        font-size: 1.4rem;
        max-width: 700px;
        margin: 0 auto 40px;
        opacity: 0.9;
        line-height: 1.6;
    }

    .feature-card {
        background: var(--white);
        border-radius: 20px;
        padding: 30px;
        height: 100%;
        box-shadow: var(--shadow);
        transition: var(--transition);
        border: 2px solid transparent;
        text-align: center;
        position: relative;
        overflow: hidden;
    }

    .feature-card:hover {
        transform: translateY(-10px);
        border-color: var(--primary-color);
        box-shadow: 0 15px 40px rgba(44, 90, 160, 0.15);
    }

    .feature-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
        background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
    }

    .card-icon {
        width: 80px;
        height: 80px;
        background: var(--secondary-color);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 25px;
        font-size: 2.2rem;
        color: var(--primary-color);
        transition: var(--transition);
    }

    .feature-card:hover .card-icon {
        background: var(--primary-color);
        color: var(--white);
        transform: scale(1.1);
    }

    .feature-card h3 {
        color: var(--primary-color);
        font-weight: 700;
        margin-bottom: 15px;
        font-size: 1.5rem;
    }

    .feature-card p {
        color: var(--text-light);
        line-height: 1.7;
        margin-bottom: 20px;
    }

    .btn-custom {
        background: var(--primary-color);
        color: white;
        padding: 12px 30px;
        border-radius: 50px;
        font-weight: 600;
        border: none;
        transition: var(--transition);
        text-decoration: none;
        display: inline-block;
    }

    .btn-custom:hover {
        background: #1a3a6d;
        color: white;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(44, 90, 160, 0.3);
    }

    .stats-section {
        background: var(--secondary-color);
        padding: 60px 0;
        margin: 80px 0;
        border-radius: 30px;
    }

    .stat-item {
        text-align: center;
        padding: 20px;
    }

    .stat-number {
        font-size: 2.8rem;
        font-weight: 800;
        color: var(--primary-color);
        display: block;
        margin-bottom: 10px;
    }

    .stat-label {
        color: var(--text-light);
        font-weight: 600;
        font-size: 1.1rem;
    }

    .cta-section {
        text-align: center;
        padding: 60px 0;
        background: linear-gradient(135deg, #f8fafc 0%, #e9f2f9 100%);
        border-radius: 30px;
        margin-top: 40px;
    }

    .section-title {
        color: var(--primary-color);
        font-weight: 800;
        margin-bottom: 40px;
        font-size: 2.2rem;
        position: relative;
        display: inline-block;
    }

    .section-title::after {
        content: "";
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: var(--accent-color);
        border-radius: 2px;
    }

    @@media (max-width: 768px) {
        .hero-title {
            font-size: 2.5rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
        }

        .hero-section {
            padding: 60px 0;
        }
    }
</style>

<!-- Hero Section -->
<div class="hero-section">
    <div class="container">
        <div class="row justify-content-center text-center">
            <div class="col-lg-10">
                <h1 class="hero-title">Online Sınav Sistemine Hoş Geldiniz</h1>
                <p class="hero-subtitle">Profesyonel, güvenli ve etkili sınav deneyimi. Öğretmenler ve öğrenciler için modern çözümler.</p>
                <a href="#" class="btn btn-light btn-lg px-5 py-3 fw-bold rounded-pill" style="background: var(--accent-color); border-color: var(--accent-color);">
                    Hemen Başla →
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Features Section -->
<div class="container">
    <h2 class="section-title text-center">Sistem Özellikleri</h2>

    <div class="row g-4 justify-content-center mt-4">
        <div class="col-lg-4 col-md-6">
            <div class="feature-card">
                <div class="card-icon">
                    👨‍🎓
                </div>
                <h3>Öğrenciler İçin</h3>
                <p>Kolay arayüz ile sınavlara katılın, anında sonuçlarınızı görüntüleyin ve detaylı performans analizlerinizi takip edin. Zaman yönetimi araçları ile daha verimli çalışın.</p>
                <a href="#" class="btn-custom">Öğrenci Girişi</a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="feature-card">
                <div class="card-icon">
                    👨‍🏫
                </div>
                <h3>Öğretmenler İçin</h3>
                <p>Zengin soru bankası ile sınav oluşturun, otomatik değerlendirme yapın, detaylı raporlar alın ve öğrenci performanslarını kolayca yönetin.</p>
                <a href="#" class="btn-custom">Öğretmen Paneli</a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="feature-card">
                <div class="card-icon">
                    📊
                </div>
                <h3>Analiz & Raporlama</h3>
                <p>Detaylı istatistikler, grafikler ve performans raporları ile sınav sonuçlarınızı analiz edin. Öğrenme sürecinizi optimize edin.</p>
                <a href="#" class="btn-custom">Raporları Görüntüle</a>
            </div>
        </div>
    </div>
</div>

<!-- Stats Section -->
<div class="stats-section">
    <div class="container">
        <div class="row text-center">
            <div class="col-md-3 col-6">
                <div class="stat-item">
                    <span class="stat-number">5,000+</span>
                    <span class="stat-label">Aktif Öğrenci</span>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="stat-item">
                    <span class="stat-number">250+</span>
                    <span class="stat-label">Öğretmen</span>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="stat-item">
                    <span class="stat-number">10,000+</span>
                    <span class="stat-label">Tamamlanan Sınav</span>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="stat-item">
                    <span class="stat-number">%99.8</span>
                    <span class="stat-label">Memnuniyet Oranı</span>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- CTA Section -->
<div class="container">
    <div class="cta-section">
        <h2 class="section-title">Hemen Ücretsiz Deneyin</h2>
        <p class="lead mb-4" style="max-width: 700px; margin: 0 auto 30px; color: var(--text-light);">
            Online sınav sistemimizi ücretsiz deneyin ve eğitim süreçlerinizi nasıl dönüştürebileceğinizi görün.
        </p>
        <div class="d-flex flex-wrap justify-content-center gap-3">
            <a href="#" class="btn btn-primary btn-lg px-5 py-3 fw-bold rounded-pill">30 Gün Ücretsiz Dene</a>
            <a href="#" class="btn btn-outline-primary btn-lg px-5 py-3 fw-bold rounded-pill">Demo İzle</a>
        </div>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    // Basit animasyon için
    document.addEventListener('DOMContentLoaded', function() {
        const cards = document.querySelectorAll('.feature-card');
        cards.forEach((card, index) => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';

            setTimeout(() => {
                card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
                card.style.opacity = '1';
                card.style.transform = 'translateY(0)';
            }, 300 + (index * 200));
        });

        // İstatistik sayma animasyonu
        const statNumbers = document.querySelectorAll('.stat-number');
        statNumbers.forEach(stat => {
            const target = parseInt(stat.textContent);
            let current = 0;
            const increment = target / 50;
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                }
                stat.textContent = current.toFixed(current % 1 === 0 ? 0 : 1);
            }, 30);
        });
    });
</script>